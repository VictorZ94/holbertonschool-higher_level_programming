#!/usr/bin/node
// to print multi string using Console
let iter = 0;
const myArray = ['C is fun', 'Python is cool', 'Javascript is amazing'];
while (iter < myArray.length) {
  console.log(myArray[iter]);
  iter++;
}
